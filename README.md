server-scripts
==============

Collection of bash-scripts to ease administration of my server.

Installation
------------

Simply run the provided `install` script which links everything to your `~/bin` directory.
